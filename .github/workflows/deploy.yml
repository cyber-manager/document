name: Build and Deploy

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout source repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Docusaurus
        run: npm run build

      - name: Test build output
        id: test-build-output
        run: |
          echo "🧪 Testing build output..."
          
          if [ ! -d "build" ]; then
            echo "❌ Build directory not found!"
            exit 1
          fi
          
          if [ ! -f "build/index.html" ]; then
            echo "❌ index.html not found in build directory!"
            exit 1
          fi
          
          if [ ! -d "build/assets" ]; then
            echo "⚠️ Assets directory not found, but continuing..."
          fi
          
          INDEX_SIZE=$(stat -f%z build/index.html 2>/dev/null || stat -c%s build/index.html 2>/dev/null || echo "0")
          if [ "$INDEX_SIZE" -lt 1000 ]; then
            echo "⚠️ index.html seems too small ($INDEX_SIZE bytes), but continuing..."
          fi
          
          echo "📁 Build directory contents:"
          ls -lah build/ | head -15
          
          echo "✅ Build structure looks good!"
          echo "📊 Build statistics:"
          echo "   - Total files: $(find build -type f | wc -l)"
          echo "   - Total size: $(du -sh build | cut -f1)"
          
          if grep -q "<!DOCTYPE html>" build/index.html || grep -q "<html" build/index.html; then
            echo "✅ HTML structure looks valid"
          else
            echo "⚠️ HTML structure might be invalid, but continuing..."
          fi
          
          echo "🌐 Testing HTTP server..."
          cd build
          python3 -m http.server 8080 > /dev/null 2>&1 &
          SERVER_PID=$!
          sleep 2
          
          if curl -f -s http://localhost:8080 > /dev/null; then
            echo "✅ HTTP server test passed - build is serving correctly"
          else
            echo "⚠️ HTTP server test failed, but build files exist"
          fi
          
          kill $SERVER_PID 2>/dev/null || true
          cd ..

      - name: Send Telegram notification on test failure
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_CHANNEL_LINK: ${{ secrets.TELEGRAM_CHANNEL_LINK }}
        run: |
          if [ -z "$TELEGRAM_BOT_TOKEN" ] || [ -z "$TELEGRAM_CHAT_ID" ]; then
            exit 0
          fi
          
          CHANNEL_INFO=""
          if [ -n "$TELEGRAM_CHANNEL_LINK" ]; then
            CHANNEL_INFO="\n📢 *Channel:* $TELEGRAM_CHANNEL_LINK"
          fi
          
          MESSAGE="❌ *Test Failed*\n\n📦 *Repository:* \`${{ github.repository }}\`\n🌿 *Branch:* \`${{ github.ref_name }}\`\n👤 *Author:* ${{ github.actor }}\n💬 *Commit:* \`${{ github.event.head_commit.message }}\`\n🔗 *Commit SHA:* \`${{ github.sha }}\`$CHANNEL_INFO\n\n❌ *Build tests failed*\n🔍 *Failed Step:* Test build output\n\n📋 *Possible reasons:*\n• Build directory not found\n• index.html missing\n• Invalid HTML structure\n• HTTP server test failed\n\n🔗 *View Logs:* ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          
          curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
            -d chat_id="$TELEGRAM_CHAT_ID" \
            -d text="$MESSAGE" \
            -d parse_mode="Markdown" \
            -d disable_web_page_preview=true

      - name: Deploy to target repository
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
          TARGET_REPO: ${{ secrets.TARGET_REPO }}
          TARGET_BRANCH: ${{ secrets.TARGET_BRANCH }}
        run: |
          TARGET_BRANCH="${TARGET_BRANCH:-main}"
          echo "🚀 Starting deployment to $TARGET_REPO:$TARGET_BRANCH"
          
          if git clone --branch $TARGET_BRANCH --single-branch https://x-access-token:$DEPLOY_TOKEN@github.com/$TARGET_REPO.git target-repo 2>&1; then
            echo "✅ Branch $TARGET_BRANCH exists, cloned successfully"
          else
            echo "⚠️ Branch $TARGET_BRANCH doesn't exist, cloning default branch..."
            git clone --single-branch https://x-access-token:$DEPLOY_TOKEN@github.com/$TARGET_REPO.git target-repo
          fi
          
          cd target-repo
          
          if git checkout $TARGET_BRANCH 2>&1; then
            echo "✅ Switched to existing branch $TARGET_BRANCH"
          else
            echo "⚠️ Branch doesn't exist, creating new branch $TARGET_BRANCH..."
            git checkout -b $TARGET_BRANCH
          fi
          
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          
          echo "🗑️ Removing existing files..."
          find . -mindepth 1 -maxdepth 1 ! -name '.git' -exec rm -rf {} +
          
          echo "📋 Copying build files..."
          cp -r ../build/* .
          cp -r ../build/.[!.]* . 2>/dev/null || true
          
          echo "🔍 Verifying files..."
          ls -la | head -20
          
          echo "➕ Adding changes..."
          git add -A .
          
          COMMIT_MSG="${{ github.event.head_commit.message }}"
          if [ -z "$COMMIT_MSG" ]; then
            COMMIT_MSG="Deploy from ${{ github.repository }}@${{ github.sha }}"
          fi
          
          echo "💬 Commit message: $COMMIT_MSG"
          
          echo "📊 Git status:"
          git status --short
          
          echo "💾 Committing changes..."
          git commit -m "$COMMIT_MSG" || {
            echo "⚠️ No changes detected, but pushing anyway to ensure sync..."
            git commit --allow-empty -m "$COMMIT_MSG"
          }
          
          echo "📤 Pushing to $TARGET_BRANCH..."
          git push origin $TARGET_BRANCH
          echo "✅ Deployment successful to $TARGET_REPO:$TARGET_BRANCH"

      - name: Send Telegram notification on PR
        if: github.event_name == 'pull_request'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_CHANNEL_LINK: ${{ secrets.TELEGRAM_CHANNEL_LINK }}
        run: |
          if [ -z "$TELEGRAM_BOT_TOKEN" ] || [ -z "$TELEGRAM_CHAT_ID" ]; then
            exit 0
          fi
          
          CHANNEL_INFO=""
          if [ -n "$TELEGRAM_CHANNEL_LINK" ]; then
            CHANNEL_INFO="\n📢 *Channel:* $TELEGRAM_CHANNEL_LINK"
          fi
          
          MESSAGE="📝 *New Pull Request*\n\n📦 *Repository:* \`${{ github.repository }}\`\n🔢 *PR Number:* #${{ github.event.pull_request.number }}\n📋 *Title:* ${{ github.event.pull_request.title }}\n👤 *Author:* ${{ github.event.pull_request.user.login }}\n🌿 *Branch:* \`${{ github.event.pull_request.head.ref }}\` → \`${{ github.event.pull_request.base.ref }}\`\n📝 *Description:*\n${{ github.event.pull_request.body }}$CHANNEL_INFO\n\n🔗 *View PR:* ${{ github.event.pull_request.html_url }}"
          
          curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
            -d chat_id="$TELEGRAM_CHAT_ID" \
            -d text="$MESSAGE" \
            -d parse_mode="Markdown" \
            -d disable_web_page_preview=true

      - name: Send Telegram notification
        if: always()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_CHANNEL_LINK: ${{ secrets.TELEGRAM_CHANNEL_LINK }}
        run: |
          if [ -z "$TELEGRAM_BOT_TOKEN" ] || [ -z "$TELEGRAM_CHAT_ID" ]; then
            exit 0
          fi
          
          CHANNEL_INFO=""
          if [ -n "$TELEGRAM_CHANNEL_LINK" ]; then
            CHANNEL_INFO="\n📢 *Channel:* $TELEGRAM_CHANNEL_LINK"
          fi
          
          if [ "${{ job.status }}" == "success" ]; then
            MESSAGE="🎉 *Deployment Successful*\n\n📦 *Repository:* \`${{ github.repository }}\`\n🌿 *Branch:* \`${{ github.ref_name }}\`\n👤 *Author:* ${{ github.actor }}\n💬 *Commit:* \`${{ github.event.head_commit.message }}\`\n🔗 *Commit SHA:* \`${{ github.sha }}\`\n📅 *Time:* $(date '+%Y-%m-%d %H:%M:%S UTC')$CHANNEL_INFO\n\n✅ Build completed successfully\n✅ Tests passed\n✅ Deployed to: \`${{ secrets.TARGET_REPO }}\`\n\n🔗 *View Run:* ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          else
            FAILED_STEP="Unknown"
            if [ -f "$GITHUB_STEP_SUMMARY" ]; then
              FAILED_STEP=$(grep -i "error\|failed" "$GITHUB_STEP_SUMMARY" | head -1 || echo "Check logs")
            fi
            
            MESSAGE="❌ *Deployment Failed*\n\n📦 *Repository:* \`${{ github.repository }}\`\n🌿 *Branch:* \`${{ github.ref_name }}\`\n👤 *Author:* ${{ github.actor }}\n💬 *Commit:* \`${{ github.event.head_commit.message }}\`\n🔗 *Commit SHA:* \`${{ github.sha }}\`\n📅 *Time:* $(date '+%Y-%m-%d %H:%M:%S UTC')$CHANNEL_INFO\n\n❌ *Status:* Build or deployment failed\n🔍 *Failed Step:* $FAILED_STEP\n\n📋 *Possible reasons:*\n• Build errors\n• Test failures\n• Deployment issues\n• Missing dependencies\n\n🔗 *View Logs:* ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          fi
          
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            PR_INFO="\n📝 *Pull Request:* #${{ github.event.pull_request.number }}\n📋 *PR Title:* ${{ github.event.pull_request.title }}"
            MESSAGE="$MESSAGE$PR_INFO"
          fi
          
          curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
            -d chat_id="$TELEGRAM_CHAT_ID" \
            -d text="$MESSAGE" \
            -d parse_mode="Markdown" \
            -d disable_web_page_preview=true
          
          echo "📱 Telegram notification sent"
